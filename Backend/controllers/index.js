const userController = require('./user');
const foodItemController = require('./foodItem');
const preferenceController = require('./preference');
const followsController = require('./follows');

module.exports = {
    userController,
    foodItemController,
    preferenceController,
    followsController
}